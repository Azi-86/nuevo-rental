# Pending / Future Work

- **Airbnb iCal sync**: automatically pull Airbnb bookings into the `blocked_dates` table using Airbnb's iCal export URL. This keeps the website calendar in sync when bookings come in via Airbnb. Free feature — no extra cost.
